<%= form_for(@funtion) do |f| %>
  <% if @funtion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@funtion.errors.count, "error") %> prohibited this funtion from being saved:</h2>

      <ul>
      <% @funtion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :movie_id %><br>
    <%= f.number_field :movie_id %>
  </div>
  <div class="field">
    <%= f.label :hora %><br>
    <%= f.time_select :hora %>
  </div>
  <div class="field">
    <%= f.label :sala %><br>
    <%= f.text_field :sala %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
